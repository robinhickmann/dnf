events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    access_log off;

    server {
        listen 80;
        location / {
            root /usr/share/nginx/html;
            index index.html;
        }
    }
}
